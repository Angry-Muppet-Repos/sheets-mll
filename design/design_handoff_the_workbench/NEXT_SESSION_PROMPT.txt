THE WORKBENCH — CHECKLIST v3 REWORK (paste this whole block as the first message in Claude Code)
================================================================================================

REPO: Angry-Muppet-Repos/sheets-mll · work on the branch this session
designates (PR #3 "The Workbench v1 — script + mockups" is open; your
commits land on it). Never main. Never force-push.

CONTEXT: The last session shipped a checklist redesign I rejected
(commit f899a6a): it replaced the horizontal products-as-rows /
steps-as-columns checklist with a vertical one-row-per-step tab. I want
the HORIZONTAL checklist — I only ever wanted it readable and
customizable. The full contract is in
design/design_handoff_the_workbench/05_revision_brief_checklist_v2.md.

PHASE 0 — ORIENT (no building)
1. Read CLAUDE.md, then design/design_handoff_the_workbench/ in full —
   05_revision_brief_checklist_v2.md is the contract; 02/03/04/06
   currently describe the REJECTED v2 layout and will need re-truing.
2. Skim apps_script/ColumnCo_Workbench_v1.gs (current = rejected v2) and
   git show 3f499e9:apps_script/ColumnCo_Workbench_v1.gs (the v1
   horizontal matrix). Run node tools/verify_workbench.js — keep this
   harness green throughout; update its contracts as the design changes.
3. Reply with your recommended design for a horizontal, readable,
   template-customizable checklist (the brief outlines Option A template
   sections vs Option B one-active-template — pick one or propose
   better) + open questions. WAIT for my decision.

PHASE 1 — VISUAL MOCKUP (after my decision)
4. Mock the chosen checklist UX in design/ui_kits/the_workbench/ BEFORE
   touching the .gs — this gate was skipped last time and is now
   mandatory. Commit + push. Tell me to open index.html. WAIT for my
   visual approval; iterate here.

PHASE 2 — THE SCRIPT (after visual approval)
5. Implement in apps_script/ColumnCo_Workbench_v1.gs. Keep: the template
   library + intake form + save-as-template plumbing from f899a6a (the
   features were right; only the checklist layout was wrong), the
   ensureGrid_/forEachSlab_ grid lessons, mock/blank mode discipline,
   generic Digital Product 30 as the default template.
6. STATIC VERIFICATION (required, show results): full
   tools/verify_workbench.js suite green under grid enforcement — parse,
   formula balance, cross-ref audit, mode audit, mock integrity,
   functional intake/save tests.
7. True up 02/03/04/06 to the v3 design in the same commit. Commit, push
   to PR #3, and send me the .gs file to download.

PHASE 3 — HANDBACK
8. Paste-and-build instructions + updated QA checklist + known
   limitations (no live execution claims — I do live QA).

HARD RULES
- The horizontal tick-across-a-row checklist is the requirement. Do not
  ship a vertical per-step list as the primary surface.
- Step names must be readable at a glance (angled headers / hover notes
  / step key — whatever the mockup proves out).
- Brand rules are non-negotiable (no exclamation points, middle-dot
  separators, no pure black/white, Forest/Canopy/Gold chrome locked).
- Do not modify ColumnCo_Foundation_v2.gs or ColumnCo_Ledger_v1.gs.
- You cannot execute Apps Script or render Sheets — never claim tested
  beyond the static verification.
